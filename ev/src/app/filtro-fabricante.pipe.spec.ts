import { FiltroFabricantePipe } from './filtro-fabricante.pipe';

describe('FiltroFabricantePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFabricantePipe();
    expect(pipe).toBeTruthy();
  });
});
