import { PatternFilterPipe } from './pattern-filter.pipe';

describe('PatternFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PatternFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
